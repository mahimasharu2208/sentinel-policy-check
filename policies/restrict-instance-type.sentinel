import "tfplan/v2" as tfplan

# Rule: EC2 instances must always be t2.micro
main = rule {
    all tfplan.planned_values.resources.aws_instance as _, instances {
        all instances as _, r {
            r.values.instance_type is "t2.micro"
        }
    }
}

